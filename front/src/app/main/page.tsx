import { ExpBar } from "@/components/main/ExpBar";
import React from "react";

const page = () => {
  return (
    <div className="w-full h-screen bg-gradient-main">
      <ExpBar />
    </div>
  );
};

export default page;
