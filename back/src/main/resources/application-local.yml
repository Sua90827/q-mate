spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mariadb://localhost:3306/qmate
    username: root
    password: qwer1234!@#$
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      connect-timeout: 2s
      timeout: 2s
  mail:
    host: smtp.gmail.com
    port: 587
    username: kimsuasch@gmail.com
    password: xksj eqev dqrb keva
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

app:
  email-verification:
    jwt-secret: "random0jwt0secret0keyaaaaaaaaaaaaaa!!!!!!!!!11111111!@#$"
    ttl-minutes: 10

security:
  jwt:
    secret: "this-is-a-DEV-secret-with-at-least-32-characters-OKaaaaaaaa!!!!!!!"
    access-ttl-seconds: 900       # 15분
    refresh-ttl-seconds: 1209600  # 14일

server:
  port: 8080